import { Box } from '@mui/material';
import styled from '@emotion/styled';

export const RowBox = styled(Box)`
  width: 100%;
  display: flex;
  flex-direction: row;
  align-items: center;
  overflow: hidden;
  white-space: nowrap;
`;
